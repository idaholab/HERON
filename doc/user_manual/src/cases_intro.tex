Each HERON analysis is referred to as a ``case''. The \xmlNode{Case} node is used to describe the
general elements of the analyis, such as the firm economics, dispatch time stepping, and so forth.

% OLD %
HERON relies on this \xmlNode{xml} node which informs the algorithm as to how the case has to be
processed by using the predefined metrics described in the following sections.
